# Block-Malicious-IP-s
This project demonstrates the implementation of a **Linux-based firewall** using **iptables** to control inbound and outbound network traffic.   The objective is to **restrict unauthorized access**, **reduce attack surface**, and **validate firewall effectiveness** using network scanning tools.
